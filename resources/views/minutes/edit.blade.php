<x-app-layout>
    <x-slot name="header"><div class="flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p class="text-sm font-semibold text-emerald-700">{{ $minute->number }}</p><h2 class="mt-1 text-2xl font-bold">Editar borrador del acta</h2></div><a href="{{ route('minutes.show',$minute) }}" class="text-sm font-bold text-slate-600 hover:text-emerald-700">← Volver al acta</a></div></x-slot>
    <div class="py-8"><div class="mx-auto max-w-4xl px-4 sm:px-6"><form method="POST" action="{{ route('minutes.update',$minute) }}" class="space-y-6">@csrf @method('PUT')<section class="rounded-2xl bg-white p-5 shadow-sm ring-1 ring-slate-200 sm:p-6"><div class="grid gap-5 sm:grid-cols-2"><div class="sm:col-span-2"><x-input-label for="title" value="Nombre o asunto de la reunión"/><x-text-input id="title" name="title" value="{{ old('title',$minute->title) }}" class="mt-1 block w-full" required/></div><div><x-input-label for="held_at" value="Fecha y hora"/><x-text-input id="held_at" name="held_at" type="datetime-local" value="{{ old('held_at',$minute->held_at->format('Y-m-d\TH:i')) }}" class="mt-1 block w-full" required/></div><div><x-input-label for="area_id" value="Área"/><select id="area_id" name="area_id" class="mt-1 block w-full rounded-xl border-slate-300" required>@foreach($areas as $area)<option value="{{ $area->id }}" @selected(old('area_id',$minute->area_id)==$area->id)>{{ $area->name }}</option>@endforeach</select></div><div><x-input-label for="organizer" value="Organizada por"/><x-text-input id="organizer" name="organizer" value="{{ old('organizer',$minute->organizer) }}" class="mt-1 block w-full"/></div><div><x-input-label for="location" value="Lugar o medio"/><x-text-input id="location" name="location" value="{{ old('location',$minute->location) }}" class="mt-1 block w-full"/></div>@foreach(['objective'=>'Objetivo','agenda'=>'Agenda / temas tratados','development'=>'Desarrollo','decisions'=>'Conclusiones y decisiones'] as $field=>$label)<div class="sm:col-span-2"><x-input-label :for="$field" :value="$label"/><textarea id="{{ $field }}" name="{{ $field }}" rows="{{ $field==='development' ? 7 : 3 }}" class="mt-1 block w-full rounded-xl border-slate-300">{{ old($field,$minute->{$field}) }}</textarea></div>@endforeach<div class="sm:col-span-2"><x-input-label value="Participantes usuarios de CUMPLE"/><div class="mt-2 grid max-h-60 gap-2 overflow-y-auto sm:grid-cols-2">@foreach($users as $user)<label class="flex items-center gap-2 rounded-xl border p-3 text-sm"><input type="checkbox" name="attendees[]" value="{{ $user->id }}" class="rounded text-emerald-700" @checked(in_array($user->id,old('attendees',$minute->attendees->pluck('id')->all())))>{{ $user->name }}</label>@endforeach</div></div><div class="sm:col-span-2"><x-input-label for="external_participant_names" value="Participantes externos (uno por línea)"/><textarea id="external_participant_names" name="external_participant_names" rows="3" class="mt-1 block w-full rounded-xl border-slate-300">{{ old('external_participant_names',collect($minute->external_participants)->pluck('name')->join("\n")) }}</textarea></div></div></section><div class="flex justify-end"><x-primary-button>Guardar borrador</x-primary-button></div></form></div></div>
</x-app-layout>
