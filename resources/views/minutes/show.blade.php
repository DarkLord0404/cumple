<x-app-layout><x-slot name="header"><div class="flex justify-between"><div><p class="text-sm font-semibold text-emerald-700">{{ $minute->number }}</p><h2 class="mt-1 text-2xl font-bold">{{ $minute->title }}</h2></div>@if($minute->generated_document_path)<a href="{{ route('minutes.download',$minute) }}" class="rounded-xl bg-emerald-700 px-4 py-2 text-sm font-bold text-white">Descargar Word</a>@endif</div></x-slot><div class="py-10"><div class="mx-auto max-w-6xl space-y-6 px-4">@if(session('status'))<div class="rounded-xl bg-emerald-50 p-4 text-sm font-bold text-emerald-800">{{ session('status') }}</div>@endif @if($errors->any())<div class="rounded-xl bg-rose-50 p-4 text-sm text-rose-800">{{ $errors->first() }}</div>@endif<section class="rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200"><div class="grid gap-5 sm:grid-cols-3"><div><p class="text-xs font-bold uppercase text-slate-500">Fecha</p><p>{{ $minute->held_at->format('d/m/Y H:i') }}</p></div><div><p class="text-xs font-bold uppercase text-slate-500">Área</p><p>{{ $minute->area?->name }}</p></div><div><p class="text-xs font-bold uppercase text-slate-500">Lugar</p><p>{{ $minute->location ?: '—' }}</p></div><div class="sm:col-span-3"><p class="text-xs font-bold uppercase text-slate-500">Objetivo</p><p class="mt-1 whitespace-pre-line">{{ $minute->objective ?: '—' }}</p></div><div class="sm:col-span-3"><p class="text-xs font-bold uppercase text-slate-500">Desarrollo</p><p class="mt-1 whitespace-pre-line leading-7">{{ $minute->development ?: '—' }}</p></div></div></section><section class="rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200"><h3 class="text-lg font-bold">Compromisos</h3><div class="mt-4 space-y-2">@foreach($minute->tasks as $task)<div class="flex justify-between rounded-xl border p-4"><div><p class="font-semibold">{{ $task->title }}</p><p class="text-sm text-slate-500">{{ $task->responsible_name }}</p></div><p class="text-sm font-bold">{{ $task->due_at?->format('d/m/Y') }}</p></div>@endforeach</div><form x-data="{ type: 'internal' }" method="POST" action="{{ route('minutes.commitments.store',$minute) }}" class="mt-6 grid gap-4 sm:grid-cols-2">@csrf<div class="sm:col-span-2"><x-input-label for="title" value="Nuevo compromiso"/><x-text-input id="title" name="title" class="mt-1 block w-full" required/></div><select name="assignee_type" x-model="type" class="rounded-xl border-slate-300"><option value="internal">Usuario CUMPLE</option><option value="external">Responsable externo</option></select><input name="due_at" type="date" class="rounded-xl border-slate-300" required><select x-show="type==='internal'" name="assigned_to" class="rounded-xl border-slate-300"><option value="">Responsable…</option>@foreach($users as $user)<option value="{{ $user->id }}">{{ $user->name }}</option>@endforeach</select><x-text-input x-show="type==='external'" name="external_assignee_name" placeholder="Nombre externo"/><div class="sm:col-span-2 flex justify-end"><x-secondary-button>Agregar compromiso</x-secondary-button></div></form></section><section class="rounded-2xl bg-emerald-950 p-6 text-white"><h3 class="text-lg font-bold">Documento institucional</h3><p class="mt-2 text-sm text-emerald-100">Se generará una copia Word usando la plantilla oficial. El borrador de CUMPLE no reemplaza el documento que debe enviarse a Calidad.</p><form method="POST" action="{{ route('minutes.generate',$minute) }}" class="mt-5">@csrf<x-primary-button>Generar Word institucional</x-primary-button></form></section></div></div></x-app-layout>
