<x-app-layout><x-slot name="header"><div class="flex items-center justify-between"><div><p class="text-sm font-semibold text-emerald-700">Gestión de reuniones</p><h2 class="mt-1 text-2xl font-bold">Actas y compromisos</h2></div><a href="{{ route('minutes.create') }}" class="rounded-xl bg-emerald-700 px-4 py-2 text-sm font-semibold text-white">Nueva acta</a></div></x-slot><div class="py-10"><div class="mx-auto max-w-7xl px-4 sm:px-6"><div class="rounded-2xl bg-white shadow-sm ring-1 ring-slate-200"><div class="border-b p-6 text-sm text-slate-600">Los borradores se organizan en CUMPLE; el Word institucional descargado es el documento que se envía a Calidad.</div><div class="divide-y">@forelse($minutes as $minute)<a href="{{ route('minutes.show',$minute) }}" class="flex items-center justify-between gap-4 p-5 hover:bg-slate-50"><div><p class="font-bold text-emerald-800">{{ $minute->number }} · {{ $minute->title }}</p><p class="mt-1 text-sm text-slate-500">{{ $minute->held_at->format('d/m/Y H:i') }} · {{ $minute->area?->name }} · {{ $minute->tasks->count() }} compromisos</p></div><span class="rounded-full bg-slate-100 px-3 py-1 text-xs font-bold">{{ $minute->status === 'ready' ? 'Word generado' : 'Borrador' }}</span></a>@empty<div class="p-12 text-center text-slate-500">No hay actas registradas.</div>@endforelse</div></div><div class="mt-5">{{ $minutes->links() }}</div></div></div></x-app-layout>
