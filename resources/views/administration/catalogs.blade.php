<x-app-layout><x-slot name="header"><div><p class="text-sm font-semibold text-emerald-700">Administración</p><h2 class="mt-1 text-2xl font-bold">Áreas y fuentes</h2></div></x-slot><div class="py-10"><div class="mx-auto max-w-7xl space-y-6 px-4 sm:px-6">@if(session('status'))<div class="rounded-xl bg-emerald-50 p-4 text-sm font-bold text-emerald-800">{{ session('status') }}</div>@endif @if($errors->any())<div class="rounded-xl bg-rose-50 p-4 text-sm text-rose-800">{{ $errors->first() }}</div>@endif<div class="grid gap-6 lg:grid-cols-2"><section class="rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200"><h3 class="text-lg font-bold">Áreas y servicios</h3><form method="POST" action="{{ route('areas.store') }}" class="mt-4 grid gap-3 sm:grid-cols-2">@csrf<input name="name" placeholder="Nombre del área" class="rounded-xl border-slate-300" required><select name="coordinator_id" class="rounded-xl border-slate-300"><option value="">Sin coordinador</option>@foreach($coordinators as $user)<option value="{{ $user->id }}">{{ $user->name }}</option>@endforeach</select><textarea name="description" placeholder="Descripción opcional" class="rounded-xl border-slate-300 sm:col-span-2"></textarea><div class="sm:col-span-2"><x-primary-button>Crear área</x-primary-button></div></form><div x-data="{ editing: null }" class="mt-6 divide-y">@foreach($areas as $area)<div class="py-3"><div class="flex items-center justify-between"><div><p class="font-semibold">{{ $area->name }}</p><p class="text-xs text-slate-500">{{ $area->coordinator?->name ?? 'Sin coordinador' }} · {{ $area->is_active ? 'Activa' : 'Inactiva' }}</p></div><button @click="editing=editing==={{ $area->id }}?null:{{ $area->id }}" class="text-sm font-bold text-emerald-700">Editar</button></div><form x-show="editing==={{ $area->id }}" x-cloak method="POST" action="{{ route('areas.update',$area) }}" class="mt-3 grid gap-2 rounded-xl bg-slate-50 p-3 sm:grid-cols-2">@csrf @method('PATCH')<input name="name" value="{{ $area->name }}" class="rounded-xl border-slate-300"><select name="coordinator_id" class="rounded-xl border-slate-300"><option value="">Sin coordinador</option>@foreach($coordinators as $user)<option value="{{ $user->id }}" @selected($area->coordinator_id==$user->id)>{{ $user->name }}</option>@endforeach</select><input name="description" value="{{ $area->description }}" class="rounded-xl border-slate-300"><select name="is_active" class="rounded-xl border-slate-300"><option value="1" @selected($area->is_active)>Activa</option><option value="0" @selected(!$area->is_active)>Inactiva</option></select><div class="sm:col-span-2"><x-secondary-button>Guardar</x-secondary-button></div></form></div>@endforeach</div></section><section class="rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200"><h3 class="text-lg font-bold">Fuentes de hallazgos</h3><form method="POST" action="{{ route('sources.store') }}" class="mt-4 grid gap-3 sm:grid-cols-[1fr_auto]">@csrf<input name="name" placeholder="Nombre de la fuente" class="rounded-xl border-slate-300" required><select name="is_invima" class="rounded-xl border-slate-300"><option value="0">General</option><option value="1">INVIMA</option></select><div class="sm:col-span-2"><x-primary-button>Crear fuente</x-primary-button></div></form><div x-data="{ editing: null }" class="mt-6 divide-y">@foreach($sources as $source)<div class="py-3"><div class="flex items-center justify-between"><div><p class="font-semibold">{{ $source->name }}</p><p class="text-xs text-slate-500">{{ $source->is_invima ? 'INVIMA · Causa–efecto obligatorio' : 'General' }} · {{ $source->is_active ? 'Activa' : 'Inactiva' }}</p></div><button @click="editing=editing==={{ $source->id }}?null:{{ $source->id }}" class="text-sm font-bold text-emerald-700">Editar</button></div><form x-show="editing==={{ $source->id }}" x-cloak method="POST" action="{{ route('sources.update',$source) }}" class="mt-3 grid gap-2 rounded-xl bg-slate-50 p-3 sm:grid-cols-3">@csrf @method('PATCH')<input name="name" value="{{ $source->name }}" class="rounded-xl border-slate-300 sm:col-span-3"><select name="is_invima" class="rounded-xl border-slate-300"><option value="0" @selected(!$source->is_invima)>General</option><option value="1" @selected($source->is_invima)>INVIMA</option></select><select name="is_active" class="rounded-xl border-slate-300"><option value="1" @selected($source->is_active)>Activa</option><option value="0" @selected(!$source->is_active)>Inactiva</option></select><div><x-secondary-button>Guardar</x-secondary-button></div></form></div>@endforeach</div></section></div></div></div></x-app-layout>
