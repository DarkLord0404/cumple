<x-app-layout>
    <x-slot name="header"><div class="flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p class="text-sm font-semibold text-emerald-700">Configuración</p><h2 class="mt-1 text-2xl font-bold text-slate-900">Fuentes de hallazgos</h2><p class="mt-1 text-sm text-slate-500">Administra los orígenes disponibles al crear oportunidades.</p></div><a href="{{ route('administration.catalogs') }}" class="text-sm font-bold text-slate-600 hover:text-emerald-700">← Volver a configuración</a></div></x-slot>
    <div class="py-6 sm:py-8" x-data="{ creating: {{ $errors->any() ? 'true' : 'false' }}, editing: null }"><div class="mx-auto max-w-6xl space-y-5 px-4 sm:px-6 lg:px-8">
        @if(session('status'))<div class="rounded-xl bg-emerald-50 p-4 text-sm font-bold text-emerald-800 ring-1 ring-emerald-200">{{ session('status') }}</div>@endif
        @if($errors->any())<div class="rounded-xl bg-rose-50 p-4 text-sm text-rose-800 ring-1 ring-rose-200">{{ $errors->first() }}</div>@endif
        <section class="overflow-hidden rounded-2xl bg-white shadow-sm ring-1 ring-slate-200">
            <div class="flex flex-col gap-4 border-b border-slate-200 px-4 py-5 sm:flex-row sm:items-center sm:justify-between sm:px-6"><div class="flex items-center gap-4"><span class="flex h-11 w-11 shrink-0 items-center justify-center rounded-xl bg-emerald-100 text-lg font-black text-emerald-700">{{ $sources->count() }}</span><div><h3 class="font-bold text-slate-900">Fuentes configuradas</h3><p class="mt-0.5 text-sm text-slate-500">{{ $sources->where('is_active',true)->count() }} activas · {{ $sources->where('is_invima',true)->count() }} tipo INVIMA</p></div></div><button type="button" @click="creating=true" class="inline-flex w-full items-center justify-center gap-2 rounded-xl bg-emerald-700 px-4 py-2.5 text-sm font-bold text-white shadow-sm hover:bg-emerald-800 sm:w-auto"><span class="text-lg leading-none">＋</span>Nueva fuente</button></div>
            <div class="hidden md:block"><table class="w-full table-fixed text-left"><thead class="bg-slate-50 text-xs font-bold uppercase tracking-wide text-slate-500"><tr><th class="w-[46%] px-6 py-3">Fuente</th><th class="w-[22%] px-4 py-3">Análisis</th><th class="w-[17%] px-4 py-3">Estado</th><th class="w-[15%] px-6 py-3 text-right">Acción</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($sources as $source)<tr class="transition hover:bg-slate-50/80"><td class="px-6 py-4"><div class="flex min-w-0 items-center gap-3"><span class="flex h-9 w-9 shrink-0 items-center justify-center rounded-lg {{ $source->is_invima ? 'bg-amber-100 text-amber-700' : 'bg-emerald-50 text-emerald-700' }} font-black">{{ mb_strtoupper(mb_substr($source->name,0,1)) }}</span><p class="truncate font-bold text-slate-900" title="{{ $source->name }}">{{ $source->name }}</p></div></td><td class="px-4 py-4"><span class="inline-flex rounded-full px-2.5 py-1 text-xs font-bold {{ $source->is_invima ? 'bg-amber-50 text-amber-700 ring-1 ring-amber-200' : 'bg-slate-100 text-slate-600' }}">{{ $source->is_invima ? 'INVIMA' : 'General' }}</span></td><td class="px-4 py-4"><span class="inline-flex items-center gap-1.5 text-sm font-semibold {{ $source->is_active ? 'text-emerald-700' : 'text-slate-500' }}"><span class="h-2 w-2 rounded-full {{ $source->is_active ? 'bg-emerald-500' : 'bg-slate-300' }}"></span>{{ $source->is_active ? 'Activa' : 'Inactiva' }}</span></td><td class="px-6 py-4 text-right"><button type="button" @click="editing={{ $source->id }}" class="rounded-lg px-3 py-2 text-sm font-bold text-emerald-700 hover:bg-emerald-50">Editar</button></td></tr>@empty<tr><td colspan="4" class="px-6 py-14 text-center text-sm text-slate-500">Aún no hay fuentes configuradas.</td></tr>@endforelse</tbody></table></div>
            <div class="divide-y divide-slate-100 md:hidden">@forelse($sources as $source)<article class="p-4"><div class="flex items-start gap-3"><span class="flex h-10 w-10 shrink-0 items-center justify-center rounded-xl {{ $source->is_invima ? 'bg-amber-100 text-amber-700' : 'bg-emerald-50 text-emerald-700' }} font-black">{{ mb_strtoupper(mb_substr($source->name,0,1)) }}</span><div class="min-w-0 flex-1"><h3 class="break-words font-bold text-slate-900">{{ $source->name }}</h3><div class="mt-2 flex flex-wrap items-center gap-2"><span class="rounded-full px-2 py-0.5 text-[11px] font-bold {{ $source->is_invima ? 'bg-amber-50 text-amber-700' : 'bg-slate-100 text-slate-600' }}">{{ $source->is_invima ? 'INVIMA' : 'General' }}</span><span class="inline-flex items-center gap-1 text-xs font-semibold {{ $source->is_active ? 'text-emerald-700' : 'text-slate-500' }}"><span class="h-1.5 w-1.5 rounded-full {{ $source->is_active ? 'bg-emerald-500' : 'bg-slate-300' }}"></span>{{ $source->is_active ? 'Activa' : 'Inactiva' }}</span></div></div><button type="button" @click="editing={{ $source->id }}" class="shrink-0 rounded-lg bg-emerald-50 px-3 py-2 text-sm font-bold text-emerald-700">Editar</button></div></article>@empty<div class="p-10 text-center text-sm text-slate-500">Aún no hay fuentes configuradas.</div>@endforelse</div>
        </section>
    </div>
    <div x-show="creating" x-cloak class="fixed inset-0 z-50 overflow-y-auto" @keydown.escape.window="creating=false"><div class="flex min-h-full items-center justify-center p-4"><div class="fixed inset-0 bg-slate-950/60 backdrop-blur-sm" @click="creating=false"></div><section class="relative w-full max-w-lg rounded-2xl bg-white shadow-2xl"><div class="flex items-start justify-between border-b border-slate-200 px-5 py-4"><div><p class="text-sm font-semibold text-emerald-700">Nuevo registro</p><h3 class="mt-1 text-xl font-bold text-slate-900">Crear fuente</h3></div><button type="button" @click="creating=false" class="rounded-lg p-2 text-2xl leading-none text-slate-400 hover:bg-slate-100">×</button></div><form method="POST" action="{{ route('sources.store') }}" class="space-y-5 p-5">@csrf<div><x-input-label for="new_source_name" value="Nombre de la fuente"/><input id="new_source_name" name="name" value="{{ old('name') }}" class="mt-1 block w-full rounded-xl border-slate-300" required></div><div><x-input-label for="new_source_type" value="Tipo de análisis"/><select id="new_source_type" name="is_invima" class="mt-1 block w-full rounded-xl border-slate-300"><option value="0">General</option><option value="1">INVIMA · causa–efecto</option></select><p class="mt-2 text-xs leading-5 text-slate-500">Esta clasificación define el tratamiento disponible al registrar un hallazgo.</p></div><div class="flex justify-end gap-3 border-t border-slate-100 pt-4"><button type="button" @click="creating=false" class="rounded-xl px-4 py-2 text-sm font-bold text-slate-600">Cancelar</button><x-primary-button>Crear fuente</x-primary-button></div></form></section></div></div>
    @foreach($sources as $source)<div x-show="editing==={{ $source->id }}" x-cloak class="fixed inset-0 z-50 overflow-y-auto" @keydown.escape.window="editing=null"><div class="flex min-h-full items-center justify-center p-4"><div class="fixed inset-0 bg-slate-950/60 backdrop-blur-sm" @click="editing=null"></div><section class="relative w-full max-w-lg rounded-2xl bg-white shadow-2xl"><div class="flex items-start justify-between border-b border-slate-200 px-5 py-4"><div><p class="text-sm font-semibold text-emerald-700">Editar configuración</p><h3 class="mt-1 max-w-sm break-words text-xl font-bold text-slate-900">{{ $source->name }}</h3></div><button type="button" @click="editing=null" class="rounded-lg p-2 text-2xl leading-none text-slate-400 hover:bg-slate-100">×</button></div><form method="POST" action="{{ route('sources.update',$source) }}" class="space-y-5 p-5">@csrf @method('PATCH')<div><x-input-label value="Nombre"/><input name="name" value="{{ $source->name }}" class="mt-1 block w-full rounded-xl border-slate-300" required></div><div class="grid gap-4 sm:grid-cols-2"><div><x-input-label value="Tipo de análisis"/><select name="is_invima" class="mt-1 block w-full rounded-xl border-slate-300"><option value="0" @selected(!$source->is_invima)>General</option><option value="1" @selected($source->is_invima)>INVIMA</option></select></div><div><x-input-label value="Estado"/><select name="is_active" class="mt-1 block w-full rounded-xl border-slate-300"><option value="1" @selected($source->is_active)>Activa</option><option value="0" @selected(!$source->is_active)>Inactiva</option></select></div></div><div class="flex justify-end gap-3 border-t border-slate-100 pt-4"><button type="button" @click="editing=null" class="rounded-xl px-4 py-2 text-sm font-bold text-slate-600">Cancelar</button><x-secondary-button type="submit">Guardar cambios</x-secondary-button></div></form></section></div></div>@endforeach
    </div>
</x-app-layout>
