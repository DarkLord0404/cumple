<x-app-layout>
    <x-slot name="header"><div class="flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p class="text-sm font-semibold text-emerald-700">Configuración</p><h2 class="mt-1 text-2xl font-bold text-slate-900">Áreas y servicios</h2><p class="mt-1 text-sm text-slate-500">Organiza la estructura y sus coordinadores responsables.</p></div><a href="{{ route('administration.catalogs') }}" class="text-sm font-bold text-slate-600 hover:text-emerald-700">← Volver a configuración</a></div></x-slot>
    <div class="py-6 sm:py-8" x-data="{ creating: {{ $errors->any() ? 'true' : 'false' }}, editing: null }"><div class="mx-auto max-w-6xl space-y-5 px-4 sm:px-6 lg:px-8">
        @if(session('status'))<div class="rounded-xl bg-emerald-50 p-4 text-sm font-bold text-emerald-800 ring-1 ring-emerald-200">{{ session('status') }}</div>@endif
        @if($errors->any())<div class="rounded-xl bg-rose-50 p-4 text-sm text-rose-800 ring-1 ring-rose-200">{{ $errors->first() }}</div>@endif
        <section class="overflow-hidden rounded-2xl bg-white shadow-sm ring-1 ring-slate-200"><div class="flex flex-col gap-4 border-b border-slate-200 px-4 py-5 sm:flex-row sm:items-center sm:justify-between sm:px-6"><div class="flex items-center gap-4"><span class="flex h-11 w-11 shrink-0 items-center justify-center rounded-xl bg-emerald-100 text-lg font-black text-emerald-700">{{ $areas->count() }}</span><div><h3 class="font-bold text-slate-900">Áreas configuradas</h3><p class="mt-0.5 text-sm text-slate-500">{{ $areas->where('is_active',true)->count() }} activas · {{ $areas->whereNotNull('coordinator_id')->count() }} con coordinador</p></div></div><button type="button" @click="creating=true" class="inline-flex w-full items-center justify-center gap-2 rounded-xl bg-emerald-700 px-4 py-2.5 text-sm font-bold text-white hover:bg-emerald-800 sm:w-auto"><span class="text-lg">＋</span>Nueva área</button></div>
            <div class="grid gap-3 bg-slate-50/60 p-4 sm:grid-cols-2 lg:grid-cols-3 sm:p-5">@forelse($areas as $area)<article class="flex min-h-32 flex-col justify-between rounded-xl bg-white p-4 shadow-sm ring-1 ring-slate-200 transition hover:-translate-y-0.5 hover:shadow-md hover:ring-emerald-200"><div class="flex min-w-0 items-start gap-3"><span class="flex h-9 w-9 shrink-0 items-center justify-center rounded-lg bg-emerald-50 font-black text-emerald-700">{{ mb_strtoupper(mb_substr($area->name,0,1)) }}</span><div class="min-w-0"><h3 class="break-words pt-0.5 text-sm font-bold leading-5 text-slate-900">{{ $area->name }}</h3><p class="mt-1 truncate text-xs font-semibold {{ $area->coordinator ? 'text-slate-600' : 'text-amber-700' }}" title="{{ $area->coordinator?->name }}">{{ $area->coordinator?->name ?? 'Sin coordinador' }}</p></div></div><div class="mt-4 flex items-center justify-between gap-2"><span class="inline-flex items-center gap-1 text-[11px] font-semibold {{ $area->is_active ? 'text-emerald-700' : 'text-slate-500' }}"><span class="h-1.5 w-1.5 rounded-full {{ $area->is_active ? 'bg-emerald-500' : 'bg-slate-300' }}"></span>{{ $area->is_active ? 'Activa' : 'Inactiva' }}</span><button type="button" @click="editing={{ $area->id }}" class="shrink-0 rounded-lg px-2.5 py-1.5 text-xs font-bold text-emerald-700 hover:bg-emerald-50">Editar</button></div></article>@empty<div class="p-10 text-center text-sm text-slate-500 sm:col-span-2 lg:col-span-3">Aún no hay áreas configuradas.</div>@endforelse</div></section>
    </div>
    <div x-show="creating" x-cloak class="fixed inset-0 z-50 overflow-y-auto" @keydown.escape.window="creating=false"><div class="flex min-h-full items-center justify-center p-4"><div class="fixed inset-0 bg-slate-950/60 backdrop-blur-sm" @click="creating=false"></div><section class="relative w-full max-w-xl rounded-2xl bg-white shadow-2xl"><div class="flex justify-between border-b border-slate-200 px-5 py-4"><div><p class="text-sm font-semibold text-emerald-700">Nuevo registro</p><h3 class="mt-1 text-xl font-bold">Crear área o servicio</h3></div><button type="button" @click="creating=false" class="rounded-lg p-2 text-2xl leading-none text-slate-400">×</button></div><form method="POST" action="{{ route('areas.store') }}" class="grid gap-4 p-5 sm:grid-cols-2">@csrf<div><x-input-label value="Nombre"/><input name="name" class="mt-1 block w-full rounded-xl border-slate-300" required></div><div><x-input-label value="Coordinador"/><select name="coordinator_id" class="mt-1 block w-full rounded-xl border-slate-300"><option value="">Sin coordinador</option>@foreach($coordinators as $user)<option value="{{ $user->id }}">{{ $user->name }} · {{ $user->role_label }}</option>@endforeach</select></div><div class="sm:col-span-2"><x-input-label value="Descripción (opcional)"/><textarea name="description" rows="3" class="mt-1 block w-full rounded-xl border-slate-300"></textarea></div><div class="flex justify-end gap-3 border-t border-slate-100 pt-4 sm:col-span-2"><button type="button" @click="creating=false" class="px-4 text-sm font-bold text-slate-600">Cancelar</button><x-primary-button>Crear área</x-primary-button></div></form></section></div></div>
    @foreach($areas as $area)<div x-show="editing==={{ $area->id }}" x-cloak class="fixed inset-0 z-50 overflow-y-auto" @keydown.escape.window="editing=null"><div class="flex min-h-full items-center justify-center p-4"><div class="fixed inset-0 bg-slate-950/60 backdrop-blur-sm" @click="editing=null"></div><section class="relative w-full max-w-xl rounded-2xl bg-white shadow-2xl"><div class="flex justify-between border-b border-slate-200 px-5 py-4"><div><p class="text-sm font-semibold text-emerald-700">Editar configuración</p><h3 class="mt-1 text-xl font-bold">{{ $area->name }}</h3></div><button type="button" @click="editing=null" class="rounded-lg p-2 text-2xl leading-none text-slate-400">×</button></div><form method="POST" action="{{ route('areas.update',$area) }}" class="grid gap-4 p-5 sm:grid-cols-2">@csrf @method('PATCH')<div><x-input-label value="Nombre"/><input name="name" value="{{ $area->name }}" class="mt-1 block w-full rounded-xl border-slate-300" required></div><div><x-input-label value="Coordinador"/><select name="coordinator_id" class="mt-1 block w-full rounded-xl border-slate-300"><option value="">Sin coordinador</option>@foreach($coordinators as $user)<option value="{{ $user->id }}" @selected($area->coordinator_id==$user->id)>{{ $user->name }} · {{ $user->role_label }}</option>@endforeach</select></div><div><x-input-label value="Descripción"/><textarea name="description" rows="3" class="mt-1 block w-full rounded-xl border-slate-300">{{ $area->description }}</textarea></div><div><x-input-label value="Estado"/><select name="is_active" class="mt-1 block w-full rounded-xl border-slate-300"><option value="1" @selected($area->is_active)>Activa</option><option value="0" @selected(!$area->is_active)>Inactiva</option></select></div><div class="flex flex-col-reverse gap-3 border-t border-slate-100 pt-4 sm:col-span-2 sm:flex-row sm:items-center sm:justify-between"><button form="delete-area-{{ $area->id }}" class="text-left text-sm font-bold text-rose-700">Eliminar esta área</button><div class="flex justify-end gap-3"><button type="button" @click="editing=null" class="px-4 text-sm font-bold text-slate-600">Cancelar</button><x-secondary-button type="submit">Guardar cambios</x-secondary-button></div></div></form><form id="delete-area-{{ $area->id }}" method="POST" action="{{ route('areas.destroy',$area) }}" onsubmit="return confirm('¿Eliminar esta área?')">@csrf @method('DELETE')</form></section></div></div>@endforeach
    </div>
</x-app-layout>
