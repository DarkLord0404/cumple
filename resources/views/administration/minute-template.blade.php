<x-app-layout>
    <x-slot name="header"><div class="flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p class="text-sm font-semibold text-emerald-700">Configuración</p><h2 class="mt-1 text-2xl font-bold text-slate-900">Plantilla de actas</h2><p class="mt-1 text-sm text-slate-500">Administra el formato Word oficial de esta organización.</p></div><a href="{{ route('administration.catalogs') }}" class="text-sm font-bold text-slate-600 hover:text-emerald-700">← Volver a configuración</a></div></x-slot>
    <div class="py-6 sm:py-8"><div class="mx-auto max-w-4xl space-y-5 px-4 sm:px-6 lg:px-8">
        @if(session('status'))<div class="rounded-xl bg-emerald-50 p-4 text-sm font-bold text-emerald-800 ring-1 ring-emerald-200">{{ session('status') }}</div>@endif
        @if($errors->any())<div class="rounded-xl bg-rose-50 p-4 text-sm text-rose-800 ring-1 ring-rose-200">{{ $errors->first() }}</div>@endif
        <section class="rounded-2xl bg-white p-5 shadow-sm ring-1 ring-slate-200 sm:p-7"><div class="flex items-start gap-4"><span class="flex h-12 w-12 shrink-0 items-center justify-center rounded-xl bg-sky-100 text-2xl">▤</span><div><p class="text-xs font-bold uppercase tracking-wide text-slate-400">Plantilla en uso</p><h3 class="mt-1 break-words text-lg font-bold text-slate-900">{{ $organization->minute_template_name ?: 'Plantilla institucional predeterminada de CUMPLE' }}</h3><p class="mt-2 text-sm leading-6 text-slate-500">El archivo debe ser DOCX y conservar los marcadores institucionales. Cambiarlo no modifica las versiones Word ya generadas.</p></div></div><form method="POST" enctype="multipart/form-data" action="{{ route('minute-template.update') }}" class="mt-6 rounded-xl bg-slate-50 p-4">@csrf @method('PUT')<x-input-label for="minute_template" value="Seleccionar nueva plantilla DOCX"/><input id="minute_template" name="template" type="file" accept=".docx" required class="mt-2 block w-full max-w-full text-sm"><div class="mt-4"><x-primary-button>Guardar plantilla</x-primary-button></div></form>@if($organization->minute_template_path)<form method="POST" action="{{ route('minute-template.destroy') }}" class="mt-5 border-t border-slate-100 pt-5" onsubmit="return confirm('¿Restaurar la plantilla predeterminada?')">@csrf @method('DELETE')<button class="text-sm font-bold text-rose-700">Restaurar plantilla predeterminada</button></form>@endif</section>
    </div></div>
</x-app-layout>
