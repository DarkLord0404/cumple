<x-app-layout>
    <x-slot name="header"><div class="flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p class="text-sm font-semibold text-emerald-700">Configuración</p><h2 class="mt-1 text-2xl font-bold text-slate-900">Organización</h2><p class="mt-1 text-sm text-slate-500">Personaliza cómo se identifica tu espacio de trabajo.</p></div><a href="{{ route('administration.catalogs') }}" class="text-sm font-bold text-slate-600 hover:text-emerald-700">← Volver a configuración</a></div></x-slot>
    <div class="py-6 sm:py-8"><div class="mx-auto max-w-4xl space-y-5 px-4 sm:px-6 lg:px-8">
        @if(session('status'))<div class="rounded-xl bg-emerald-50 p-4 text-sm font-bold text-emerald-800 ring-1 ring-emerald-200">{{ session('status') }}</div>@endif
        @if($errors->any())<div class="rounded-xl bg-rose-50 p-4 text-sm text-rose-800 ring-1 ring-rose-200">{{ $errors->first() }}</div>@endif
        <section class="overflow-hidden rounded-2xl bg-white shadow-sm ring-1 ring-slate-200"><div class="border-b border-slate-200 bg-gradient-to-r from-emerald-950 to-emerald-800 p-6 text-white sm:p-8"><span class="flex h-12 w-12 items-center justify-center rounded-2xl bg-white/10 text-2xl ring-1 ring-white/20">🏢</span><p class="mt-5 text-sm font-semibold text-emerald-200">Identidad de la organización</p><h3 class="mt-1 break-words text-2xl font-extrabold">{{ $organization?->name }}</h3><p class="mt-2 max-w-xl text-sm leading-6 text-emerald-100/80">Este nombre permite que cada usuario reconozca el espacio al que pertenece.</p></div><form method="POST" action="{{ route('organization.update') }}" class="p-6 sm:p-8">@csrf @method('PATCH')<div class="max-w-2xl"><x-input-label for="organization_name" value="Nombre visible de la organización"/><input id="organization_name" name="name" value="{{ old('name', $organization?->name) }}" maxlength="255" class="mt-2 block w-full rounded-xl border-slate-300 text-base font-semibold" required><p class="mt-2 text-xs leading-5 text-slate-500">El cambio se reflejará en el menú principal de todos los usuarios. La información y los accesos no se modifican.</p><div class="mt-6"><x-primary-button>Guardar nombre</x-primary-button></div></div></form></section>
    </div></div>
</x-app-layout>
