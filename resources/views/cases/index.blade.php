<x-app-layout>
    <x-slot name="header"><div class="flex items-center justify-between"><div><p class="text-sm font-semibold text-emerald-700">Gestión interna</p><h2 class="mt-1 text-2xl font-bold text-slate-900">Hallazgos y planes</h2></div><a href="{{ route('cases.create') }}" class="rounded-xl bg-emerald-700 px-4 py-2 text-sm font-semibold text-white hover:bg-emerald-800">Registrar hallazgo</a></div></x-slot>
    <div class="py-10"><div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8"><div class="rounded-2xl bg-white shadow-sm ring-1 ring-slate-200">
        <div class="border-b border-slate-100 p-6"><p class="text-sm text-slate-600">CUMPLE organiza el seguimiento. Los archivos Word y Excel institucionales continúan siendo los documentos oficiales.</p></div>
        <div class="overflow-x-auto"><table class="min-w-full text-sm"><thead class="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr><th class="px-6 py-3">Caso</th><th class="px-6 py-3">Origen</th><th class="px-6 py-3">Área</th><th class="px-6 py-3">Acciones</th><th class="px-6 py-3">Estado</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($cases as $case)<tr class="hover:bg-slate-50"><td class="px-6 py-4"><a class="font-bold text-emerald-800" href="{{ route('cases.show',$case) }}">{{ $case->code }}</a><p class="mt-1 text-slate-700">{{ $case->title }}</p></td><td class="px-6 py-4">{{ $case->source->name }}</td><td class="px-6 py-4">{{ $case->reportingArea->name }}</td><td class="px-6 py-4">{{ $case->tasks->count() }}</td><td class="px-6 py-4"><span class="rounded-full bg-amber-50 px-2 py-1 text-xs font-semibold text-amber-800">Borrador</span></td></tr>@empty<tr><td colspan="5" class="px-6 py-14 text-center text-slate-500">Todavía no hay hallazgos registrados.</td></tr>@endforelse</tbody></table></div>
    </div><div class="mt-5">{{ $cases->links() }}</div></div></div>
</x-app-layout>
